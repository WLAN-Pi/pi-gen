# Wi-Fi is managed by wlanpi-core (wpa_supplicant/hostapd, including network
# namespaces), not NetworkManager. NetworkManager only manages the wired NIC.
# Without this, NM tries to activate a supplicant and marks every radio
# "unavailable", which also spams the journal.
[keyfile]
unmanaged-devices=type:wifi
